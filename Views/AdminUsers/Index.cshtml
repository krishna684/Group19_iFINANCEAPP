@model IEnumerable<Group19_iFINANCEAPP.Models.UserPassword>

@{
    ViewBag.Title = "All Users";
}

<h2 class="mb-4 text-light">All Registered Users</h2>

<div class="form-group mb-4">
    <label for="searchBox" class="form-label text-light">Search Users by Username</label>
    <input type="text" id="searchBox" class="form-control bg-dark text-light border-secondary" placeholder="Enter username..." />
</div>

<div id="userResults">
    @Html.Partial("_UserTable", Model)
</div>

@section Scripts {
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script>
        $(document).ready(function () {
            $("#searchBox").on("keyup", function () {
                var query = $(this).val();
                $.get("@Url.Action("Search", "AdminUsers")", { searchString: query }, function (data) {
                    $("#userResults").html(data);
                });
            });
        });
    </script>
}
