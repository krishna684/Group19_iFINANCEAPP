@model IEnumerable<Group19_iFINANCEAPP.Models.TransactionLine>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CreditedAmount)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DebitedAmount)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Comments)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MasterAccount.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TransactionHeader.Description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CreditedAmount)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DebitedAmount)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Comments)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MasterAccount.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TransactionHeader.Description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
